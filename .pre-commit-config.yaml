ci:
  autoupdate_commit_msg: "chore: update pre-commit hooks"
  autofix_commit_msg: "style: pre-commit fixes"
  autofix_prs: true
  autoupdate_schedule: weekly

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: "v5.0.0"
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
        args: ['--maxkb=1000']
      - id: check-merge-conflict
        types: [text]
      - id: check-toml
      - id: check-json
      - id: debug-statements
      - id: mixed-line-ending
      - id: name-tests-test
        args: ["--pytest-test-first"]

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: "v0.15.2"
    hooks:
      - id: ruff
        args: ["--fix", "--show-fixes"]
      - id: ruff-format

  - repo: https://github.com/igorshubovych/markdownlint-cli
    rev: "v0.47.0"
    hooks:
      - id: markdownlint-fix
        args: ["--config", ".markdownlint.json"]
        exclude: ^\.github/(instructions|skills)/

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: "v1.19.1"
    hooks:
      - id: mypy
        additional_dependencies:
          - types-PyYAML
          - types-tqdm
        args: [--ignore-missing-imports]
        exclude: ^(tests/|scripts/|yolo_cam/)

  - repo: local
    hooks:
      - id: pylint-check
        name: pylint
        entry: uv run pylint
        language: system
        types: [python]
        args: ["--fail-under=9.0"]
        exclude: ^(yolo_cam/|\.venv/)

      - id: pytest-check
        name: pytest-check
        entry: uv run pytest
        language: system
        pass_filenames: false
        always_run: true
        args: ["-x", "--tb=short", "-q"]
        stages: [pre-push]
